{
  "name": "smart-model-router",
  "version": "0.0.1",
  "description": "A project that implements a smart router gateway for AI models using OpenRouter.",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file .env --inspect --watch src/index.ts",
    "test:dev": "node --env-file .env --inspect --watch --test ./tests/**/*.test.ts",
    "test": "node --env-file .env --test  ./tests/**/*.test.ts"
  },
  "keywords": [],
  "author": "rafael@dellaquila.dev",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@openrouter/sdk": "^0.5.1",
    "@types/node": "^24.10.12",
    "fastify": "^5.7.4"
  }
}
